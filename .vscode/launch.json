{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "hostname": "0.0.0.0",  // WSL2では必要
            "port": 9004,  // Xdebug のポートを9004に設定
            "pathMappings": {
                "/data/app": "${workspaceFolder}/src/app"  // コンテナ内のパスをローカルのパスにマッピング
            },
            "log": true
        }
    ]
}